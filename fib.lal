#(defun 'fib 'x 'y
#  (let 'n (+ x y)
#    (body
#      (print n)
#      (if (< n 100)
#        (fib y n)
#      )
#    )
#  )
#)
#
#(print 0)
#(print 1)
#(fib 0 1)

(let 'a 1 'b (+ "fo" "o") 'c "bar"
  (let 'c (+ c "abc")
    (print
      (+ a (+ 2 3))
      (+ b c)
    )
  )
)
